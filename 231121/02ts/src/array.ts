let array = new Array();
array.push(1);
array.push(2);
array.push(3);

// console.log(array);

let numArray: number[] = [1, 2, 3];
let strArray: string[] = ["hello", "world"];
interface Iperson {
  name: string;
  age?: number;
}
type Iperson01 {
  name: string;
  age?: number;
}
let personArray = Iperson01[] = [{ name: "jack" }, { name: "june", age: 32 }];

배열이 중요한 이우, 배열객체가 갖고 있는 속성!
배열 = inter