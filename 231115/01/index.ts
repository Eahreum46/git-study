let n: number = 1;
let b: boolean = true;
let s: string = "hello";
let o: object = {};

//모든 타입(*숫자,문자, 논리 등)을 초월하는 타입!
let a: any = 0;
a = "hello";

//JS에서 undefined => 변수안에 값이 지정되지않았을때
//TS에서는 undefined 하나의 타입

let o: object{
  name:"park",
  age: 100,
}

o2={
  first: 1,
  secn
}